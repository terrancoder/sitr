node_modules/
tools/compiler/dist/
extension/dist/
tests/dist/

# Compiled rulesets are build output (recompiled in CI from blocklist/),
# but they are committed so the shipped artifact is reviewable in the repo.
# Do not add extension/rulesets/ here.

# Internal working notes — not part of the public repo.
# Public docs live in docs/ and ARE committed.
docspriv/

# Chrome writes indexed ruleset caches here when loading the unpacked extension
extension/_metadata/

# Packed store zips (npm run pack)
build/

# Internal working agreement — not part of the public repo
CLAUDE.md
