<?xml version="1.0" encoding="utf-8"?>
<!-- The permissions we declare. Deliberately absent, and to stay that
     way: QUERY_ALL_PACKAGES, Accessibility, Device Admin, camera,
     location, storage.

     Note the MERGED manifest is larger than this list: androidx.work
     adds WAKE_LOCK (it holds a wakelock while a worker runs) and
     androidx.core adds the signature-level, self-scoped
     <pkg>.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION. Verify what a build
     actually ships with:
       adb shell dumpsys package com.sitrshield.sitr | sed -n '/requested permissions/,/install permissions/p'
     Any claim about permissions must describe THAT list, not this one. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".SitrApp"
        android:label="Sitr"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- systemExempted is the documented FGS type for VpnService apps;
             lint's check only models the exact-alarm path, hence the
             suppression. No alarm permissions are requested — the
             permission surface stays minimal. -->
        <service
            android:name="com.sitrshield.engine.SitrVpnService"
            android:exported="false"
            android:foregroundServiceType="systemExempted"
            android:permission="android.permission.BIND_VPN_SERVICE"
            tools:ignore="ForegroundServicePermission">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".boot.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Managed configurations (EMM / Family Link) — the storage.managed
             analog; read via RestrictionsManager, strongest gate layer. -->
        <meta-data
            android:name="android.content.APP_RESTRICTIONS"
            android:resource="@xml/app_restrictions" />
    </application>
</manifest>
